name: CodeQL

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  codeql-go:
    uses: chargehound/security-workflows-public/.github/workflows/codeql-go.yml@main